# PyQt6 with pre-built binaries (no C++ compiler needed)
PyQt6>=6.4.0

# Modern dark theme - using compatible version for Python 3.13
pyqtdarktheme>=0.1.7,<2.0.0

# Data validation and serialization
pydantic>=2.0.0

# Password hashing
bcrypt>=4.0.0

# Build tools (for creating executables)
pyinstaller>=6.0.0
pillow>=10.0.0
